git config --global user.name "santosh-acl"
git config --global user.password "Acldigital@2024"
git remote add origin git@github.com:santosh-acl/DEMO.git

git remote remove origin
git remote -v


🔄 Change Remote URL (e.g., Switch from SSH to HTTPS)
git remote set-url origin https://github.com/santosh-acl/DEMO.git

Remove a Remote:
git remote remove origin

 Re-add Remote (if removed):
git remote add origin git@github.com:santosh-acl/DEMO.git


🚀 Test SSH Connection
ssh -T git@github.com
ssh-keygen -t ed25519 -C "yesg210@gmail.com"

4️⃣ Add SSH Key to GitHub
Copy the SSH key to the clipboard:
Edit
type C:\Users\Santosh.Katiyar\.ssh\id_ed25519.pub
Copy the output (starts with ssh-ed25519 ...)
Go to GitHub SSH Keys
Click "New SSH Key", paste the key, and save it.
5️⃣ Test SSH Connection Again
Run: ssh -T git@github.com

ssh -T git@github.com
If successful, you’ll see:
https://github.com/santosh-acl/DEMO.git